---
title: "Rust 数组类型"
type: type-reference
category: 基础类型
tags:
    - rust
    - array
    - compound
related:
    - 向量
    - 切片
    - 元组
---

# Rust 数组类型

数组 `[T; N]` 是 Rust 中将多个相同类型的值分配在栈上的集合类型。与向量不同，**数组的长度是编译时已知且不可变的**，这使得它非常适合存放固定大小的数据。

## 一、速览

| 特性 | 说明 |
| :--- | :--- |
| 存储位置 | 栈 |
| 长度 | 编译时固定，无法在运行时增长或缩小 |
| 元素类型 | 必须全部相同 |
| 是否拥有数据 | 是，拥有所有权 |
| 默认值 | 取决于元素类型（整数默认 `0`，浮点默认 `0.0` 等） |

## 二、数组操作

数组的类型签名为 `[T; N]`，其中 `T` 是元素类型，`N` 是编译时常量。

```rust
    // 显式声明类型
    let arr1: [i32; 4] = [1, 2, 3, 4];

    // 类型自动推断
    let arr2 = [1, 2, 3];

    // 创建包含相同初始值的数组
    let zeros = [0; 5];        // [0, 0, 0, 0, 0]
    let truth = [true; 3];     // [true, true, true]

    // 索引访问，索引类型必须是 usize
    let mut arr = [1, 2, 3, 4, 5];
    let first = arr[0];
    arr[0] = 10;  // 修改需要 mut

    // 安全访问，返回 Option<&T>
    let maybe = arr.get(1);  // Some(&2)
    let none = arr.get(10);  // None

    // 遍历
    for element in &arr {
        println!("{}", element);
    }

    for (i, val) in arr.iter().enumerate() {
        println!("arr[{}] = {}", i, val);
    }
```

## 三、数组解构

```rust
    let arr = [1, 2, 3];

    // 解构数组
    let [a, b, c] = arr;
    // a = 1, b = 2, c = 3

    // 使用 .. 忽略剩余元素
    let [first, ..] = arr;
    // first = 1

    let [.., last] = arr;
    // last = 3
```

## 四、数组与切片的关系

数组可以自动强制转换为切片 `&[T]`，不会复制数据：

```rust
    fn analyze(slice: &[i32]) {
        println!("length: {}", slice.len());
    }

    let arr = [10, 20, 30];
    analyze(&arr);  // &[i32; 3] 自动转换为 &[i32]

    let view: &[i32] = &arr[1..4];  // [20, 30]，不复制数据
```

## 五、常用方法

数组的方法主要通过自动解引用为切片获得：

```rust
    let arr = [3, 1, 4, 1, 5];

    arr.len();         // 元素个数（5）
    arr.is_empty();    // 是否为空（false）
    arr.first();       // 第一个元素，返回 Option<&T>
    arr.last();        // 最后一个元素，返回 Option<&T>
    arr.contains(&3);  // 是否包含某个值
    arr.iter();        // 返回迭代器

    // 排序需要可变切片 &mut [T]
    let mut mut_arr = [3, 1, 4, 1, 5];
    mut_arr.sort();
```
